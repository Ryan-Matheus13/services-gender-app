import { rootSlice } from "./slice";

export const {
  handleIncrementProduct,
  handleDecrementProduct,
  handleAddProduct,
  handleResetFavorites,
  handleSetProducts,
  handleRemoveProduct,
} = rootSlice.actions;
